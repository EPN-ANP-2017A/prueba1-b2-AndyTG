{% extends 'layout.html.twig' %}

{% block body %}
    <h1 class="text-center">Ingrese la cantidad</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar" />
    {{ form_end(form) }}

    <ul>
        <li class="btn btn-primary">
            <a class="text-white" href="{{ path('pedido_index') }}">Lista de Pedidos</a>
        </li>
    </ul>
{% endblock %}
