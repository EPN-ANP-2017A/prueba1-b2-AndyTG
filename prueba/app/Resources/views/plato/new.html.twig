{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Ingresar nuevo Plato</h1>
    <div class="form-group">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" />
        {{ form_end(form) }}
    </div>

    <ul>
        <li class="btn btn-primary">
            <a class="text-white" href="{{ path('plato_index') }}">Regresar a lista de platos</a>
        </li>
    </ul>
{% endblock %}
